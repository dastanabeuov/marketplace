<div class="row">
  <%= render 'nav' %>
  <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 my-4">
    <%= render 'admin/shared/breadcrumbs' %>

    <div class="sitename-info">
      <%= render "admin/shared/attached_image", object: @sitename %>
      <h1><%= @sitename.name %></h1>
      <p><%= @sitename.description %></p>
    </div>

    <div class="btn-group mt-3">
      <%= button_to admin_sitename_path(@sitename),
          method: :delete,
          data: { turbo_confirm: t('are_you_sure') },
          class: "btn btn-danger",
          title: t('destroy') do %>
        <i class="bi bi-trash3-fill"></i>
      <% end %>

      <%= link_to edit_admin_sitename_path(@sitename), class: "btn btn-warning", title: t('edit') do %>
        <i class="bi bi-pencil-fill"></i>
      <% end %>
    </div>
  </main>
</div>
