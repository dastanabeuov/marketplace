<!-- Topbar - скрывается в мобильной версии -->
<div class="d-none d-lg-block">
  <%= render 'shared/topbar' %>
</div>

<nav class="navbar navbar-expand-lg bg-gray-200 shadow">
  <div class="container">
    <%= link_to (site_name || t('.marketplace')), root_path, class: "navbar-brand fw-bold fs-5" %>

    <!-- Кнопка для мобильного меню -->
    <div class="d-flex align-items-center gap-2 d-lg-none">
      <!-- Кнопка для топбара в мобильной версии -->
      <button class="btn btn-sm btn-outline-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#mobileTopbar" aria-controls="mobileTopbar" aria-expanded="false" aria-label="Показать контакты">
        <i class="bi bi-info-circle"></i>
      </button>
      
      <!-- Основная кнопка навигации -->
      <button class="navbar-toggler" type="button"
              data-bs-toggle="collapse"
              data-bs-target="#mainNavbar"
              aria-controls="mainNavbar"
              aria-expanded="false"
              aria-label="Toggle navigation"
              data-controller="navbar">
        <i data-navbar-target="icon" class="bi bi-list"></i>
      </button>
    </div>

    <!-- Мобильный топбар (скрытый по умолчанию) -->
    <div class="collapse d-lg-none" id="mobileTopbar">
      <div class="card mt-2 mb-3">
        <div class="card-body p-3">
          <%= render 'shared/topbar_content' %>
        </div>
      </div>
    </div>

    <div class="collapse navbar-collapse" id="mainNavbar">
      <!-- Desktop tabs -->
      <ul class="nav nav-tabs card-header-tabs me-auto mb-2 mb-lg-0 d-none d-lg-flex">
        <li class="nav-item">
          <%= nav_link "🏠 #{ I18n.t('.main_page') }", root_path %>
        </li>
        <li class="nav-item">
          <%= nav_link "ℹ️ #{ I18n.t('.about') }", about_path %>
        </li>
        <li class="nav-item">
          <%= nav_link "📂 #{ I18n.t('.catalog') }", products_path %>
        </li>

        <div class="pt-1">
          <%= dropdown_nav_link "🔧 #{ I18n.t('.mechanic') }",
                mechanics.map { |m| { path: mechanic_path(m) } } do %>
            <% mechanics.each do |mechanic| %>
              <li><%= dropdown_item_link mechanic.name, mechanic_path(mechanic) %></li>
            <% end %>
          <% end %>
        </div>

        <li class="nav-item">
          <%= nav_link "💼 #{ I18n.t('.vacancies') }", vacancies_path %>
        </li>
        <li class="nav-item">
          <%= nav_link "📞 #{ I18n.t('.contacts') }", contact_path %>
        </li>
      </ul>
      <!-- Desktop tabs -->

      <!-- Мобильная часть навигации -->      
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 d-flex d-lg-none">
        <li class="nav-item">
          <%= link_to "🏠 #{ I18n.t('.main_page') }", root_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to "ℹ️ #{ I18n.t('.about') }", about_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to "📂 #{ I18n.t('.catalog') }", products_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to "🏢 #{ I18n.t('.brands') }", companies_path, class: 'nav-link' %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="mechanicDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            🔧 <%= I18n.t('.mechanic') %>
          </a>
          <ul class="dropdown-menu" aria-labelledby="mechanicDropdown">
            <% mechanics.each do |mechanic| %>
              <li>
                <%= link_to mechanic.name, mechanic_path(mechanic), class: "dropdown-item" %>
              </li>
            <% end %>
          </ul>
        </li>
        <li class="nav-item">
          <%= link_to "💼 #{ I18n.t('.vacancies') }", vacancies_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to "📞 #{ I18n.t('.contacts') }", contact_path, class: 'nav-link' %>
        </li>
      </ul>
      <!-- Мобильная часть навигации -->      

      <ul class="navbar-nav ms-auto d-flex align-items-lg-center gap-2">
        <li class="nav-item">
          <%= render 'shared/lang' %>
        </li>
        <li class="nav-item">
          <%= render 'shared/color_modes' %>
        </li>

        <% if user_signed_in? && current_user %>
          <li class="nav-item dropdown">
            <a class="btn btn-success dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="bi bi-person-fill me-1"></i> <%= current_user.decorate.full_name %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userDropdown">
              <li>
                <%= button_to "<i class='bi bi-door-closed'></i> #{I18n.t('.sign_out')}".html_safe, destroy_user_session_path, method: :delete, class: "dropdown-item" %>
              </li>
              <li>
                <%= link_to "<i class='bi bi-send'></i> #{I18n.t('all_my_orders')}".html_safe, orders_path, class: "dropdown-item" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item d-flex flex-column flex-lg-row gap-2 mt-3 mt-lg-0">
            <%= link_to "<i class='bi bi-door-open'></i> #{I18n.t('.sign_in')}".html_safe, new_user_session_path, class: "btn btn-outline-primary" %>
            <%= link_to "<i class='bi bi-person-check'></i> #{I18n.t('.sign_up')}".html_safe, new_user_registration_path, class: "btn btn-outline-success" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>